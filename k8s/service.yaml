apiVersion: v1
kind: Service
metadata:
  name: weather-dashboard
  namespace: weather-dashboard
  labels:
    app.kubernetes.io/name: weather-dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: weather-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
